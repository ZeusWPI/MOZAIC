const Visualizer = require('./components/visualizer');
const ReactDOM = require('react-dom');
const h = require('react-hyperscript');

ReactDOM.render(h(Visualizer), document.getElementById('root'));
